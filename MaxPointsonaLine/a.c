#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{
	//char *s = (char*)malloc(sizeof(char)*1);
	//char *t = (char*)malloc(sizeof(char)*1);
	char *s= NULL, *t = NULL;
//	int n = strncmp(s, t, 1);
	printf("%d\n", s[0] == '\0');
	return 0;
}
